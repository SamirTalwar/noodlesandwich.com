extend layout.pug

block main
  section.narrow.about-me
    header
      h2 About me

    p Hi there. My name is Samir Talwar.

    p I'm an automation engineer and software crafter, living in London. My main foci are functional programming, cloud systems and containers.

    p I also teach others how to do what I do.

    p Currently, I'm off on a new venture and not looking for work. Watch this space.

  section.narrow.upcoming-events
    mixin events(title, events)
      header
        h2 Upcoming #{title}

        if events.length === 0
          p No upcoming #{title}.

        else
          each event in events
            article
              p
                a(href=(event.slug ? `/events/${event.slug}/${event.isoDate}` : event.link)) #{event.title}
                if event.partner
                  br
                  small with #[a(href=event.partner.link) #{event.partner.name}]
                br
                if event.event
                  small #{event.event} @ #{event.location}
                else
                  small #{event.location}
                br
                small #{event.formattedDate}

    +events('workshops', upcomingWorkshops)

    +events('talks', upcomingTalks)
