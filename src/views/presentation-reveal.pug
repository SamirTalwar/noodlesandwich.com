doctype html
html(lang='en')
  head
    include head.pug

    link(rel='stylesheet' href='/talks.css')

    link(rel='stylesheet' href='/vendor/prismjs/prism.css')

    script(src='https://platform.twitter.com/widgets.js' charset='utf-8' async defer)

    link(rel='stylesheet' href='/vendor/reveal.js/reveal.css')
    link(rel='stylesheet' href='/vendor/reveal.js/theme/white.css')
    script(src='/vendor/reveal.js/reveal.js' defer)
    script.
      document.addEventListener('DOMContentLoaded', () => {
        for (let node of Array.from(document.getElementsByClassName('notes'))) {
          node.parentElement.removeChild(node)
        }
        Reveal.initialize()
      })

  body.full-screen
    .talk.presentation
      .reveal
        .slides
          section
            header
              h1 #{title}
              p #{formattedDate}

          | !{contents}
