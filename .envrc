#!/bin/bash

NVM_DIR="$HOME/.nvm"
if [[ -s "$NVM_DIR/nvm.sh" ]]; then
  # shellcheck disable=SC1090
  source_env "$NVM_DIR/nvm.sh" --no-use
  nvm use
fi

if [[ -f .envrc.local ]]; then
  source_env .envrc.local
fi
